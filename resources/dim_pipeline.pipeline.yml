resources:
  pipelines:
    dim_pipeline:
      name: dim_pipelines
      catalog: workspace
      schema: mention_dw
      photon: true
      serverless: true
      channel: CURRENT
      continuous: false
      libraries:
        - glob:
            include: ../pipelines/dimensions/silver/**
        - glob:
            include: ../pipelines/dimensions/gold/**
