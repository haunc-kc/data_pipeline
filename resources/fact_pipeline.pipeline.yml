resources:
  pipelines:
    fact_pipeline:
      name: fact_pipelines
      catalog: workspace
      schema: mention_dw
      photon: true
      serverless: true
      channel: CURRENT
      continuous: false
      libraries:
        - glob:
            include: ../pipelines/facts/silver/**
        - glob:
            include: ../pipelines/facts/gold/**
